import {useEffect,useState} from "react"; import PageIntro from "../../components/ui/PageIntro"; import {decideStudentAccountRequest,getReadableErrorMessage,getStudentAccountRequests} from "../../services/api";
export default function StudentAccountRequestsPage(){const [status,setStatus]=useState("Pending"),[rows,setRows]=useState([]),[error,setError]=useState(""),[success,setSuccess]=useState("");async function load(){try{setRows(await getStudentAccountRequests(status));}catch(e){setError(getReadableErrorMessage(e));}}useEffect(()=>{load()},[status]);async function decide(id,approve){const reason=approve?"":(prompt("سبب الرفض (سيظهر للطالب):","يرجى مراجعة إدارة المؤسسة.")||"");if(!approve&&!reason)return;try{await decideStudentAccountRequest(id,approve,reason);setSuccess(approve?"تم قبول الطلب وإنشاء حساب الطالب.":"تم رفض الطلب وإبلاغ الطالب.");load()}catch(e){setError(getReadableErrorMessage(e));}}return <div className="entity-page" dir="rtl"><PageIntro title="طلبات حسابات الطلاب" description="مراجعة طلبات التسجيل الذاتي بعد تفعيل البريد الإلكتروني."/>{error&&<div className="alert error">{error}</div>}{success&&<div className="alert success">{success}</div>}<div className="entity-toolbar"><select value={status} onChange={e=>setStatus(e.target.value)}><option value="Pending">تحت الإجراء</option><option value="Approved">مقبولة</option><option value="Rejected">مرفوضة</option></select></div><div className="table-wrap"><table className="app-table"><thead><tr><th>الاسم</th><th>البريد</th><th>النوع</th><th>المرحلة</th><th>الحالة</th><th>الإجراء</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.fullName}</td><td>{x.email}</td><td>{x.gender==="Male"?"ذكر":"أنثى"}</td><td>{({Primary:"ابتدائي",Intermediate:"متوسط",Secondary:"ثانوي",University:"جامعي"})[x.educationStage]}</td><td>{x.status}</td><td>{x.status==="Pending"&&<><button className="primary-btn slim" onClick={()=>decide(x.id,true)}>قبول</button> <button className="ghost-btn slim" onClick={()=>decide(x.id,false)}>رفض</button></>}</td></tr>)}</tbody></table></div></div>}
